{
  "private": true,
  "dependencies": {
    "domready": "^1.0.8",
    "normalize.css": "^3.0.3",
    "gulp": "^3.9.0",
    "rump": "^0.7.0",
    "rump-server": "^0.7.0",
    "rump-scripts": "^0.7.0",
    "rump-static": "^0.7.0",
    "rump-styles": "^0.7.0"
  },
  "devDependencies": {
    "mocha": "^2.2.5",
    "rump-scripts-test": "^0.7.0",
    "rump-server-dev": "^0.7.0"
  },
  "scripts": {
    "start": "npm run server",
    "test": "gulp rump:test",
    "test:watch": "gulp rump:test:watch",
    "build": "gulp rump:build",
    "build:prod": "gulp rump:build:prod",
    "server": "gulp rump:server",
    "server:dev": "gulp rump:server:dev",
    "server:prod": "gulp rump:server:prod",
    "info": "gulp rump:info",
    "info:prod": "gulp rump:info:prod"
  }
}
