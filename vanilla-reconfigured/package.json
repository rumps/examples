{
  "private": true,
  "dependencies": {
    "domready": "^1.0.7",
    "normalize.css": "^3.0.2",
    "gulp": "^3.8.8",
    "rump": "^0.6.0",
    "rump-scripts": "^0.6.0",
    "rump-static": "^0.6.0",
    "rump-styles": "^0.6.0"
  },
  "devDependencies": {
    "rump-scripts-test": "^0.6.0",
    "rump-server": "^0.6.0"
  },
  "scripts": {
    "start": "npm run server",
    "test": "gulp rump:test",
    "test:watch": "gulp rump:test:watch",
    "build": "gulp rump:build",
    "build:prod": "gulp rump:build:prod",
    "server": "gulp rump:server",
    "server:prod": "gulp rump:server:prod",
    "info": "gulp rump:info",
    "info:prod": "gulp rump:info:prod"
  }
}
