{
  "private": true,
  "dependencies": {
    "domready": "^1.0.7",
    "normalize.css": "^3.0.2",
    "rump": "^0.3.0",
    "rump-scripts": "^0.3.0",
    "rump-static": "^0.3.0",
    "rump-styles": "^0.3.0"
  },
  "devDependencies": {
    "expect.js": "^0.3.1",
    "rump-scripts-test": "^0.3.1",
    "rump-server": "^0.3.0"
  },
  "scripts": {
    "start": "npm run server",
    "test": "gulp test",
    "test:watch": "gulp test:watch",
    "build": "gulp build",
    "build:prod": "npm run build:prod",
    "server": "gulp server",
    "server:prod": "gulp server:prod",
    "info": "gulp info",
    "info:prod": "gulp info:prod"
  }
}
